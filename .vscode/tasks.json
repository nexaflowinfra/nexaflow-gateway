{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run NexaFlow tests",
      "type": "shell",
      "command": "${workspaceFolder}\\.venv\\Scripts\\python.exe",
      "args": [
        "-c",
        "import test_app; [getattr(test_app, name)() for name in dir(test_app) if name.startswith('test_')]; print('tests ok')"
      ],
      "group": "test",
      "problemMatcher": []
    }
  ]
}
